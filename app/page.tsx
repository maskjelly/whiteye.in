// The persistent desktop in the root layout renders the app icons and windows.
// Keeping this route lets articles and other pages share the same desktop session.
export default function HomePage() {
  return null
}
